export interface Project {
  id: number;
  name: string;
  description: string;
  client: string;
  location: string;
  Length: string;
  impact: string;
  imageUrl: string;
}

export interface Service {
  id: number;
  title: string;
  description: string;
}